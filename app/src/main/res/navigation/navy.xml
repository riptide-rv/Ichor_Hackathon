<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navy"
    app:startDestination="@id/donorProfileFragment">

    <fragment
        android:id="@+id/donorProfileFragment"
        android:name="com.lastmin.ichor.DonorProfileFragment"
        android:label="fragment_donor_profile"
        tools:layout="@layout/fragment_donor_profile" />
</navigation>